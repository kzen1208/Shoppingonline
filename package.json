{
  "name": "shoppingonline",
  "private": true,
  "scripts": {
    "install:client-admin": "npm --prefix client-admin install",
    "install:client-customer": "npm --prefix client-customer install",
    "install:server": "npm --prefix server install",
    "install:all": "npm run install:client-admin && npm run install:client-customer && npm run install:server",
    "build:admin": "npm --prefix client-admin run build",
    "build:customer": "npm --prefix client-customer run build",
    "build": "npm run build:admin && npm run build:customer",
    "start:server": "npm --prefix server start",
    "start": "npm run install:all && npm run build && npm run start:server"
  }
}
